in the back end put your  string from mongodb and it assign to the MONGODB URL and get the terminal in Backend and type npm install and after type npm start

in the front end get a terminal in frontend and type npm install and after type npm start
